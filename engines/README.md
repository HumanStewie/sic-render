# Rendering engines for [sic]render

This is where we will establish our rendering engine (Vulkan or OpenGL), then manage them, calling draw calls. We will have an abstraction layer here, creating a nice abstraction for `main.cpp` to call.