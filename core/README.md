# The core

This is where we store all the necessary core components like mathematics, shaders, etc.

- `data/`: Structs of reusable data. Some are managed by game engine, but not all.
- `shader/`: Just the shader class